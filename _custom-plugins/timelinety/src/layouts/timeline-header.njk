    {% if header  %}
    <header class="page-header">
        <h1>{{ title }}</h1>
        <p>{{ header }}</p>
        <p>Last Update: {% humanizeDate lastUpdatedPost %}</p>
    </header>
    {% endif %}